<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="net.pingfang.dao.user.UserInfoDao">
	<!-- 根据用户名密码获取用户记录
   <select id="getUserInfo" resultType="net.pingfang.entity.user.UserInfoVo" parameterType="net.pingfang.entity.user.UserInfoVo">
	 SELECT
	 	id,
	 	userName,
	 	passWord,
	 	telephone 
	 FROM user_infor
	 WHERE userName = #{userName} AND passWord = #{passWord}
   </select>
    -->
	
</mapper>